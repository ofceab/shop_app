<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/css/main.css" rel="stylesheet" />
    <link href="<%= isAdmin ? '/css/admin.css' :'' %>" rel="stylesheet" />
    <title><%= pageTitle %></title>
</head>

<body>
    <header>
        <div class="container">
            <nav><a class="logo" href="#">MyShop</a>
                <ul>
                    <li><a href="/" class="<%= path=='/' ? 'active' :'' %>">Presentation</a></li>
                    <li><a href="/shop" class="<%= path=='/shop' ? 'active' :'' %>">Shop</a></li>
                    <li><a href="/cart" class="<%= path=='/cart' ? 'active' :'' %>">Cart</a></li>
                    <li><a href="/admin/add-product" class="<%= path=='/admin/add-product' ? 'active' :'' %>">Add Product</a></li>
                    <li><a href="/admin/products" class="<%= path=='/admin/products' ? 'active' :'' %>">Admin Product</a></li>
                </ul>
            </nav>
        </div>
    </header>