<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(href="/css/main.css", rel="stylesheet")
        block styles
        title #{pageTitle}
    body
        header
            .container
                nav
                    a(href="#", class="logo") Faham
                    ul
                        li
                            a(href="/") Presentation
                        li
                            a(href="/shop") Shop
                        li
                            a(href="/admin/add-product") Add Product

        block content 